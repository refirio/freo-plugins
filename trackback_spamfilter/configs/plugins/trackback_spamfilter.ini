;deny_url       char(50)
;deny_word      char(50)
;need_word      char(50)
;need_multibyte bool     "禁止|許可"
;need_link      bool     "必須|任意"

[スパム対策]

;投稿制限サイト(コンマ区切りで複数指定可能)
deny_url = ""

;投稿禁止ワード(コンマ区切りで複数指定可能)
deny_word = ""

;投稿必須ワード(コンマ区切りで複数指定可能)
need_word = ""

;半角英数字のみの投稿
need_multibyte = Off

;引用リンク
need_link = Off

;exit
