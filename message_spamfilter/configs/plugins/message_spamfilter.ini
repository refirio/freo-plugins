;deny_host      char(50)
;deny_word      char(50)
;need_word      char(50)
;need_multibyte bool     "禁止|許可"
;max_url        int(4)   "個"

[スパム対策]

;投稿制限対象ホスト(コンマ区切りで複数指定可能)
deny_host = "anonymizer.com,delegate"

;投稿禁止ワード(コンマ区切りで複数指定可能)
deny_word = ""

;投稿必須ワード(コンマ区切りで複数指定可能)
need_word = ""

;半角英数字のみの投稿
need_multibyte = Off

;記述できるURLの最大数
max_url = 3

;exit
